# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Node.js expense-tracking application (`aplicacion-gastos`). Uses CommonJS modules (`"type": "commonjs"` in package.json). Currently in early development — `index.js` is the only source file and serves as the entry point.

## Commands

```bash
node index.js      # Run the application
npm test           # (not yet configured)
```
